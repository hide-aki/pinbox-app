import React from 'react';

interface FileTreeProps{
    tree: object
}


export const FileTree = (props: FileTreeProps) => {
    return (
        <div>
            File Tree View
        </div>
    )
};
