import React, {useContext} from 'react';
import {Page} from '../components/Page';

export const Login: React.FC = () => {
    return (
        <Page>
            <h2>Login</h2>
        </Page>
    )
};

